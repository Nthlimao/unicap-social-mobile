export const START_CHAT = (data) => {
    return {
        type: 'START_CHAT',
        data
    }
}

export const START_SUBSCRIBE = (data) => {
    return {
        type: 'START_SUBSCRIBE',
        data
    }
}